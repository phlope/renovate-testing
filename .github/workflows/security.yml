name: Security

on:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && echo "Scan passed"

  audit:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && echo "Audit passed"

  verify:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && echo "Verify passed"
