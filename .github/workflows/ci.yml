name: CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && echo "Lint passed"

  test:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && exit 1

  build:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 2 && echo "Build passed"
